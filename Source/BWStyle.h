#pragma once
#include <QColor>

// TODO: This sucks as editing the style recompiles everything
namespace BWS {
  static QColor button = QColor(255, 255, 255);
  static QColor buttonOver = QColor(220, 220, 220);

  static QColor text = QColor(0, 0, 0);
  static QColor textLow = QColor(90, 90, 90);

  static QColor main = QColor(0, 0, 255);
}
