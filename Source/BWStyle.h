#pragma once
#include <QColor>

// TODO: This sucks as editing the style recompiles everything
namespace BWS {
  static QColor button = QColor(23, 33, 43);
  static QColor buttonOver = QColor(53, 63, 73);

  static QColor text = QColor(255, 255, 255);

  static QColor item = QColor(45, 55, 65);
  static QColor itemOver = QColor(65, 75, 85);
}
